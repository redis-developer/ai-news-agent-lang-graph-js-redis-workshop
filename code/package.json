{
  "name": "@news-agent/root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green 'npm run dev:server' 'npm run dev:web'",
    "dev:server": "npm run dev -w @news-agent/server",
    "dev:web": "npm run dev -w @news-agent/web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
